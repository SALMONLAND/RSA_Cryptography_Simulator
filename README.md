# 🔐 RSA Cryptography Simulator

> **RSA 암호화 알고리즘을 직접 체험할 수 있는 교육용 시뮬레이터**

[![Language](https://img.shields.io/badge/Language-C-blue.svg)](https://en.wikipedia.org/wiki/C_(programming_language))
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey.svg)]()

RSA(Rivest-Shamir-Adleman) 공개키 암호화 시스템의 동작 원리를 이해하고 실습할 수 있는 CLI 기반 시뮬레이터입니다. 키 생성부터 암호화/복호화까지 전 과정을 단계별로 확인할 수 있습니다.

---

## 📋 목차

- [주요 기능](#-주요-기능)
- [실행 화면](#-실행-화면)
- [설치 및 실행](#-설치-및-실행)
- [사용 방법](#-사용-방법)
- [RSA 알고리즘 설명](#-rsa-알고리즘-설명)
- [기술적 구현](#-기술적-구현)
- [제한사항](#-제한사항)
- [개발자](#-개발자)

---

## ✨ 주요 기능

### 1️⃣ RSA 키 생성
- 사용자가 직접 두 소수(p, q)를 선택
- 자동 소수 판별 및 유효성 검증
- 공개키(e, n)와 개인키(d, n) 자동 계산
- 키 생성 과정의 모든 단계 시각화

### 2️⃣ 메시지 암호화
- 평문 메시지를 RSA 공개키로 암호화
- 각 문자별 암호화 과정을 수식과 함께 출력
- ASCII 코드 → 암호문 변환 과정 추적

### 3️⃣ 메시지 복호화
- 암호문을 RSA 개인키로 복호화
- 각 암호문 블록의 복호화 과정 시각화
- 원본 메시지와의 일치 여부 자동 검증

### 4️⃣ 교육적 요소
- 모든 계산 과정을 단계별로 표시
- 수학적 수식을 함께 출력하여 이해 증진
- 오류 발생 시 명확한 안내 메시지 제공

---

## 🖥️ 실행 화면

```
╔════════════════════════════════════════╗
║  RSA 암호화 시뮬레이터 v1.0            ║
║  개발: 이관엽 (Cyber Security Project) ║
╚════════════════════════════════════════╝

┌────────────────────────────┐
│      메인 메뉴             │
└────────────────────────────┘
1. RSA 키 생성
2. 메시지 암호화
3. 메시지 복호화
4. 현재 키 정보 확인
5. 종료

선택 >>
```

### 키 생성 예시
```
[선택한 소수]
  p = 61
  q = 53

[계산 과정]
  n = p × q = 61 × 53 = 3233
  φ(n) = (p-1) × (q-1) = 60 × 52 = 3120

[공개키 (Public Key)] - 암호화에 사용
  (e, n) = (17, 3233)

[개인키 (Private Key)] - 복호화에 사용
  (d, n) = (2753, 3233)
```

---

## 🚀 설치 및 실행

### 필요 환경
- C 컴파일러 (GCC, Clang, MSVC 등)
- 표준 C 라이브러리

### 컴파일

**Linux / macOS**
```bash
gcc -o rsa_simulator rsa_simulator.c -lm
./rsa_simulator
```

**Windows (MinGW)**
```bash
gcc -o rsa_simulator.exe rsa_simulator.c
rsa_simulator.exe
```

**Windows (Visual Studio)**
```bash
cl rsa_simulator.c
rsa_simulator.exe
```

---

## 📖 사용 방법

### Step 1: RSA 키 생성
1. 메인 메뉴에서 `1` 선택
2. 첫 번째 소수 p 입력 (11~997 범위)
3. 두 번째 소수 q 입력 (p와 다른 값, 11~997 범위)
4. 공개지수 e 입력 (0 입력 시 기본값 65537 사용)
5. 생성된 공개키와 개인키 확인

### Step 2: 메시지 암호화
1. 메인 메뉴에서 `2` 선택
2. 암호화할 메시지 입력 (최대 100자)
3. 각 문자별 암호화 과정 확인

### Step 3: 메시지 복호화
1. 메인 메뉴에서 `3` 선택
2. 이전에 암호화한 메시지가 자동으로 복호화됨
3. 원본 메시지와 일치 여부 확인

### 사용 예시
```
암호화할 메시지를 입력하세요: HELLO

[문자별 암호화 과정]
문자  ASCII    수식                   암호문
────────────────────────────────────────────────
H     72       72^17 mod 3233 = 2721
E     69       69^17 mod 3233 = 1084
L     76       76^17 mod 3233 = 2897
L     76       76^17 mod 3233 = 2897
O     79       79^17 mod 3233 = 958
```

---

## 🔬 RSA 알고리즘 설명

### 수학적 원리

RSA는 **큰 소수의 소인수분해의 어려움**에 기반한 공개키 암호 시스템입니다.

#### 1. 키 생성 과정
1. **두 개의 큰 소수 선택**: p, q
2. **n 계산**: n = p × q (모듈러스)
3. **φ(n) 계산**: φ(n) = (p-1)(q-1) (오일러 함수)
4. **공개지수 e 선택**: 1 < e < φ(n), gcd(e, φ(n)) = 1
5. **개인지수 d 계산**: d ≡ e⁻¹ (mod φ(n))

#### 2. 암호화
```
암호문 C = M^e mod n
```
- M: 평문 (메시지)
- e: 공개지수
- n: 모듈러스

#### 3. 복호화
```
평문 M = C^d mod n
```
- C: 암호문
- d: 개인지수
- n: 모듈러스

### 보안 원리
- **공개키 (e, n)**는 공개되어도 안전
- **개인키 (d, n)**를 알려면 n을 소인수분해해야 함
- n이 충분히 크면(실제로는 2048비트 이상) 소인수분해가 사실상 불가능

---

## 🛠️ 기술적 구현

### 핵심 알고리즘

#### 소수 판별 (Miller-Rabin 대신 Trial Division)
```c
int isPrime(long long num) {
    // 2를 제외한 짝수는 소수가 아님
    // √num까지만 검사하여 효율성 확보
}
```

#### 최대공약수 (유클리드 호제법)
```c
long long gcd(long long a, long long b) {
    // 재귀 대신 반복문으로 구현
    // O(log min(a,b)) 시간복잡도
}
```

#### 모듈러 역원 (확장 유클리드 호제법)
```c
long long modInverse(long long e, long long phi) {
    // e × d ≡ 1 (mod φ(n))를 만족하는 d 계산
}
```

#### 모듈러 거듭제곱 (빠른 거듭제곱)
```c
long long modPow(long long base, long long exp, long long mod) {
    // O(log exp) 시간복잡도
    // 큰 지수에 대해서도 효율적으로 계산
}
```

### 자료구조
```c
typedef struct {
    long long e;  // 공개지수
    long long d;  // 개인지수
    long long n;  // 모듈러스
    long long p;  // 소수 p (디버깅용)
    long long q;  // 소수 q (디버깅용)
} RSAKey;
```

---

## ⚠️ 제한사항

### 보안상 제약
- **교육 목적으로만 사용**: 실제 보안 용도로는 부적합
- **작은 키 크기**: 소수 범위가 11~997로 제한됨
- **패딩 없음**: PKCS#1 등의 패딩 미적용

### 기술적 제약
- **오버플로우 가능성**: `long long` 범위 초과 시 오류
- **문자 제한**: ASCII 문자만 지원 (한글 등 멀티바이트 문자 미지원)
- **메시지 길이**: 최대 100자로 제한

### 권장 개선사항
실제 사용을 위해서는 다음이 필요합니다:
- 2048비트 이상의 키 크기
- 안전한 난수 생성기
- OAEP 패딩 적용
- 타이밍 공격 방어

---

## 👨‍💻 개발자

**이동혁**
- 🏫 동원동우고등학교
- 📧 Contact: [GitHub Issues](../../issues)

### 개발 배경
국정원-경기도교육청 공동 사이버보안 캠프 참여 후 암호학에 대한 이해를 심화하고, RSA 알고리즘의 동작 원리를 직접 구현해보고자 이 프로젝트를 시작했습니다.

---

## 📚 참고 자료

- [RSA 암호 시스템 (Wikipedia)](https://ko.wikipedia.org/wiki/RSA_%EC%95%94%ED%98%B8)
- [A Method for Obtaining Digital Signatures and Public-Key Cryptosystems (원논문)](https://people.csail.mit.edu/rivest/Rsapaper.pdf)
- [정수론 기초](https://en.wikipedia.org/wiki/Number_theory)
- [모듈러 연산](https://en.wikipedia.org/wiki/Modular_arithmetic)

---

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

## 🙏 Acknowledgments

- RSA 알고리즘 발명자: Ron Rivest, Adi Shamir, Leonard Adleman
- 경기도교육청 & 국가정보원 사이버보안 캠프
- 동원동우고등학교

---

**⭐ 이 프로젝트가 도움이 되셨다면 Star를 눌러주세요!**
